# UIデザイナーAI向け：テキスト改善実装依頼プロンプト

あなたは「9コマ」のUI/UXを磨き上げるシニアデザイナーです。
以下の指示に従い、プロジェクト全体のテキスト（見出し、リード文、補助テキスト等）を最新のブランドガイドラインに合わせて更新してください。

## 🎯 実装のゴール
「9コマ」という名称と、「私を構成する9つのマンガ」というキャッチコピーをサイト全体に一貫して適用し、ユーザーが迷わずに操作できるリッチなメタテキスト・マイクロコピーを実装すること。

## 📖 参照リソース（必須）
実装前に必ず以下のファイルを確認し、提案されている正確な文言を反映してください：
- **`docs/ux_seo_proposal.md`**: 言語・トーンのマスターガイド

## 🛠 具体的な作業内容

### 1. トップページ (`src/components/HomeClient.tsx`)
- **ヘッダー**:
    - H1を `9コマ` に。
    - サブ見出しを `私を構成する9つのマンガをシェアしよう` に。
- **操作ガイド**: ユーザーが9枠を埋めるための誘導メッセージ（案A）を反映。
- **入力欄のプレースホルダー**: 作成者名やテーマ入力欄のヒントを、最新のマイクロコピーに更新。
- **ドラッグ＆ドロップ案内**: マスを入れ替えられる旨の補足テキストを追加または更新。

### 2. レイアウト・メタデータ (`src/app/layout.tsx`)
- **Metadata**: `title` および `description` を `docs/ux_seo_proposal.md` の「内部設定」セクションに従って完全一致で更新。
- **Footer**: コピーライト表記等を `9コマ` に統一。

### 3. 検索モーダル (`src/components/HomeClient.tsx` 内)
- 検索窓のプレースホルダーを、著者名も探せることを示す文言に変更。
- おすすめ作品の導入文を `{theme} で多くの人が選んでいる作品：` に統一。

### 4. 詳細ページ・シェア (`src/app/list/[id]/ListViewClient.tsx`)
- ページタイトルの構成要素を修正。
- シェアボタン付近に、URLシェア（横長）と画像シェア（縦長）の違いを端的に説明するテキストを配置。

## ⚠️ 厳守ルール
1. **一貫性**: `9coma` や `自分を形作った...` などの古い表記が残っていないか、プロジェクト全体を検索して徹底的に排除してください。
2. **ブラウジング確認（絶対）**:
    実装後、必ず以下の手順を行ってください：
    - ローカルサーバー（`npm run dev`）を起動（停止している場合）。
    - **オーナーに対し「ブラウザツールを使用して、変更後のUIが意図通りか、見切れていないか、トーンが合っているか」の最終確認を必ず求めてください。** オーナーのGoサインが出るまで実装を完了としないでください。
