rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lists/{listId} {
      allow read, write: if true;
    }
    match /manga_cache/{isbn} {
      allow read, write: if true;
    }
  }
}
